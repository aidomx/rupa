#include <rupa.h>
/**
 * Runner `rupa go <file.rp>` — langkah 4 design class
 * (entry `main` + activation `@input`). Stub untuk sekarang:
 * handler "go" di loader sudah stop autorun; implementasi
 * runner menyusul.
 */
int goCompile(const char *paths[], int length) {
  (void)paths;
  (void)length;
  return 0;
}
