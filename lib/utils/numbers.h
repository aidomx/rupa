#pragma once

#if defined(RUPA_PACKAGE_H)

extern int getDouble(const char *content, int *pos);
extern int getFloat(const char *content, int *pos);
extern int getNumber(const char *content, int *pos);

#endif
